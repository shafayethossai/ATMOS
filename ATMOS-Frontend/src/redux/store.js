/* Redux store — wired for future use when API integration is needed.
   Currently the app uses useAQIData hook with local mock data.
   Add RTK slices here when connecting a real backend. */

export const store = null
